% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sce_assay_to_scipy_csc.R
\name{sce_assay_to_scipy_csc}
\alias{sce_assay_to_scipy_csc}
\title{Convert a SingleCellExperiment assay to a scipy CSC sparse matrix}
\usage{
sce_assay_to_scipy_csc(x, assay = "counts")
}
\arguments{
\item{x}{A \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment}.}

\item{assay}{A single string: the name of the assay to extract
(default \code{"counts"}).}
}
\value{
A Python \code{scipy.sparse.csc_matrix} (a reticulate Python object;
not auto-converted to R).
}
\description{
Extracts an assay from a \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment} object,
coerces it to a \link[Matrix:dgCMatrix-class]{Matrix::dgCMatrix}, and constructs a Python
\code{scipy.sparse.csc_matrix} by passing \code{\link[reticulate:np_array]{reticulate::np_array()}} views of the
underlying R memory (true zero-copy: \code{flags.owndata = FALSE}).
\subsection{Slot mapping}{\tabular{lll}{
   R (\code{dgCMatrix}) \tab Python / scipy CSC \tab dtype \cr
   \code{x@x} \tab \code{data} \tab \code{float64} \cr
   \code{x@i} \tab \code{indices} \tab \code{int32} \cr
   \code{x@p} \tab \code{indptr} \tab \code{int32} \cr
   \code{x@Dim} \tab \code{shape} \tab – \cr
}

}

\subsection{Zero-copy semantics}{

\code{\link[reticulate:np_array]{reticulate::np_array()}} uses the C buffer protocol to expose R's
contiguous memory to NumPy \strong{without copying} (\code{flags.owndata = FALSE}).
This works because plain R vectors are already C-contiguous:
\itemize{
\item R numeric (\code{double}) maps directly to NumPy \code{float64}.
\item R integer (\code{int32_t}) maps directly to NumPy \code{int32}.
}

\strong{Note on \code{np.array(..., copy=False)}}: NumPy >= 2.0 changed this flag to
raise \code{ValueError} when a copy would be needed.  Using
\code{\link[reticulate:np_array]{reticulate::np_array()}} avoids this entirely and is the canonical
reticulate API for zero-copy array creation from R.

\strong{Lifetime caveat}: the returned Python object holds a borrowed reference
to the underlying R vectors.  Do \emph{not} modify, reallocate, or allow the
\code{mat@x}, \code{mat@i}, \code{mat@p} vectors to be garbage-collected while the Python
object is alive.
}

\subsection{Why CSC?}{

\code{dgCMatrix} stores columns contiguously (Compressed Sparse Column).
We map directly to \code{csc_matrix} without reordering values.  Building a CSR
matrix would require transposing the data (= always a copy).
}

\subsection{int32 for indptr}{

R integer vectors are 32-bit (\code{int32_t}).  scipy CSC accepts \code{int32} for
both \code{indices} and \code{indptr}, supporting up to \eqn{2^{31}-1} non-zero
entries per matrix.
}
}
\examples{
\dontrun{
library(SingleCellExperiment)
library(Matrix)
counts <- rsparsematrix(200, 50, density = 0.1, repr = "C")
sce <- SingleCellExperiment(assays = list(counts = counts))
csc <- sce_assay_to_scipy_csc(sce, assay = "counts")
sp <- reticulate::import("scipy.sparse", convert = FALSE)
shape_r <- reticulate::py_to_r(csc$shape)
cat("Shape:", shape_r[[1]], "x", shape_r[[2]], "\n")
cat("Sparse:", reticulate::py_to_r(sp$issparse(csc)), "\n")
cat("owndata:", reticulate::py_to_r(csc$data$flags$owndata), "\n")
}

}
\seealso{
\code{\link[=sce_to_anndata]{sce_to_anndata()}}, \code{\link[=sce_to_reticulate_anndata]{sce_to_reticulate_anndata()}}
}
