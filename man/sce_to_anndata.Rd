% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sce_to_anndata.R
\name{sce_to_anndata}
\alias{sce_to_anndata}
\title{Convert a SingleCellExperiment to a Python AnnData object}
\usage{
sce_to_anndata(
  x,
  assay = "counts",
  obs = NULL,
  var = NULL,
  layers = list(),
  obsm = list(),
  varm = list()
)
}
\arguments{
\item{x}{A \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment}.}

\item{assay}{A single string: the assay to place in \code{X} (default
\code{"counts"}).}

\item{obs}{\code{NULL} or a \code{data.frame} of cell-level metadata.  Defaults to
\code{as.data.frame(colData(x))} with \code{rownames} set to \code{colnames(x)}.}

\item{var}{\code{NULL} or a \code{data.frame} of gene-level metadata.  Defaults to
\code{as.data.frame(rowData(x))} with \code{rownames} set to \code{rownames(x)}.}

\item{layers}{A named list of additional assay matrices to place in
\code{AnnData$layers}.}

\item{obsm}{A named list of matrices added to \code{AnnData$obsm}.}

\item{varm}{A named list of matrices added to \code{AnnData$varm}.}
}
\value{
A Python \code{anndata.AnnData} object (reticulate, \code{convert = FALSE}).
}
\description{
Creates a Python \code{anndata.AnnData} object from a
\link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment} using a
\code{scipy.sparse.csc_matrix} built with \code{\link[=sce_assay_to_scipy_csc]{sce_assay_to_scipy_csc()}}.
\subsection{Copy semantics}{
\itemize{
\item \strong{X} (the main count matrix): the dgCMatrix is first converted to a
\code{scipy.sparse.csc_matrix} (shape \verb{genes × cells}) via
\code{\link[=sce_assay_to_scipy_csc]{sce_assay_to_scipy_csc()}}, then transposed to
\code{scipy.sparse.csr_matrix} (shape \verb{cells × genes}) by \code{.T}.  The
transpose shares all data buffers with the original CSC
(\code{flags.owndata = FALSE}), so \strong{no values are copied}.
\item \strong{obs / var metadata}: a pandas \code{DataFrame} is constructed from the R
\code{data.frame}; reticulate performs one copy here, which is unavoidable for
columnar R data frames → row-oriented Python objects.
\item \strong{layers / obsm / varm}: passed through as-is; the caller is responsible
for any copy semantics of those objects.
}
}
}
\examples{
\dontrun{
sce <- SingleCellExperiment::SingleCellExperiment(
  assays = list(counts = Matrix::rsparsematrix(200, 50, 0.1, repr = "C"))
)
adata <- sce_to_anndata(sce)
cat("obs x var:", reticulate::py_str(adata$shape), "\n")
}

}
\seealso{
\code{\link[=sce_assay_to_scipy_csc]{sce_assay_to_scipy_csc()}}, \code{\link[=sce_to_reticulate_anndata]{sce_to_reticulate_anndata()}}
}
