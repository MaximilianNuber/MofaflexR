% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/python-imports.R
\name{py_mudata}
\alias{py_mudata}
\title{Import the Python \code{mudata} module (convert = FALSE)}
\usage{
py_mudata()
}
\value{
The Python \code{mudata} module (\code{convert = FALSE}).
}
\description{
Returns the Python \code{mudata} module imported with \code{reticulate::import()} and
\code{convert = FALSE}.  The result is cached so the import only happens once per
R session.

The module is useful for creating \code{MuData} objects inside tests or
interactive sessions before wrapping them with \link{ReticulateMuData}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mu    <- py_mudata()
mdata <- mu$MuData(dict_of_adatas)
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
mu <- py_mudata()
mu$`__version__`
}

}
\seealso{
\link{ReticulateMuData}, \code{\link[=reticulate_mudata]{reticulate_mudata()}}
}
