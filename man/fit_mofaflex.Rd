% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_mofaflex.R
\name{fit_mofaflex}
\alias{fit_mofaflex}
\title{Train a MOFAFLEX model}
\usage{
fit_mofaflex(
  data,
  model_options = list(),
  data_options = list(),
  training_options = list(),
  mofa_compat = "modelonly",
  smooth_options = NULL
)
}
\arguments{
\item{data}{One of:
\itemize{
\item A \link{ReticulateMuData} object (created by \code{\link[=sce_to_reticulate_mudata]{sce_to_reticulate_mudata()}},
\code{\link[=mae_to_reticulate_mudata]{mae_to_reticulate_mudata()}}, or \code{\link[=reticulate_mudata]{reticulate_mudata()}}).
\item A \code{ReticulateAnnData} object from the \strong{anndataR} package (created by
\code{\link[=sce_to_reticulate_anndata]{sce_to_reticulate_anndata()}}).
\item A Python \code{mudata.MuData} object (with \code{convert = FALSE}).
\item A Python \code{anndata.AnnData} object (with \code{convert = FALSE}).
\item A named R list of named lists, where outer names are group names,
inner names are view names, and each leaf is a Python \code{AnnData} object.
Example: \code{list(ctrl = list(rna = adata_ctrl), stim = list(rna = adata_stim))}.
}}

\item{model_options}{Named R list of model options passed to
\code{mofaflex.ModelOptions(...)}. See the \emph{Model options} section for all
available fields and their defaults.}

\item{data_options}{Named R list of data options passed to
\code{mofaflex.DataOptions(...)}. See the \emph{Data options} section.}

\item{training_options}{Named R list of training options passed to
\code{mofaflex.TrainingOptions(...)}. The \code{mofa_compat} field is \strong{always}
overridden by the top-level \code{mofa_compat} argument. See the
\emph{Training options} section.}

\item{mofa_compat}{Controls MOFA2 compatibility of the saved model file.
One of:
\itemize{
\item \code{"modelonly"} \emph{(default)} — save only model weights, readable by MOFA2.
\item \code{"full"} — save the full model state.
\item \code{FALSE} — disable MOFA2-compatible saving entirely.
}

This value is injected into \code{training_options$mofa_compat} before the
Python options object is constructed, overriding any user-supplied value.}

\item{smooth_options}{Optional named R list of smooth options passed to
\code{mofaflex.SmoothOptions(...)}, required for the MEFISTO / GP-factor
model. \code{NULL} (default) omits the smooth term entirely. See the
\emph{Smooth options} section.}
}
\value{
A Python \code{mofaflex.MOFAFLEX} object (with \code{convert = FALSE}).
Use \code{$} to call methods on the trained model. Key methods include:
\itemize{
\item \code{model$get_factors()} — latent factor scores as a dict of pandas
DataFrames or AnnData objects (controlled by \code{return_type}).
\item \code{model$get_weights()} — feature weights as a dict of pandas DataFrames.
\item \code{model$get_r2()} — variance explained as a pandas DataFrame.
\item \code{model$get_annotations()} — annotation enrichment results.
\item \code{model$get_significant_factor_annotations()} — significant factor-
programme associations.
}

To convert individual results to R, use \code{\link[reticulate:r-py-conversion]{reticulate::py_to_r()}} or the
\code{convert} helpers in the \strong{reticulate} package.
To save or reload the model use \code{model$...} methods or the
\code{mofaflex.MOFAFLEX.load()} class method.
}
\description{
Creates and trains a \code{MOFAFLEX} model using the Python \code{mofaflex} package
via \code{reticulate}. The user provides data and three named R lists describing
the data, model, and training options—corresponding directly to the Python
\code{mofaflex.DataOptions}, \code{mofaflex.ModelOptions}, and
\code{mofaflex.TrainingOptions} dataclasses.

The function always forces \code{mofa_compat} in \code{training_options} to the value
of the \code{mofa_compat} argument (default \code{"modelonly"}), so the saved model
file is readable by the MOFA2 R package without any extra steps.
}
\section{Model options}{

All fields are keyword arguments passed verbatim to \code{mofaflex.ModelOptions}.
Integer fields should be supplied as R integers (e.g., \code{n_factors = 5L}).\tabular{lll}{
   Field \tab Default \tab Description \cr
   \code{n_factors} \tab \code{0} \tab Number of factors. \code{0} means determine automatically. \cr
   \code{weight_prior} \tab \code{"Normal"} \tab Prior distribution on feature weights. One of \code{"Normal"}, \code{"Horseshoe"}, \code{"Laplace"}, \code{"SnS"}. Can be a named list mapping view names to priors. \cr
   \code{factor_prior} \tab \code{"Normal"} \tab Prior distribution on latent factors. One of \code{"Normal"}, \code{"GP"}, \code{"Horseshoe"}, \code{"Laplace"}, \code{"SnS"}. \cr
   \code{likelihoods} \tab \code{NULL} \tab Named character vector of likelihood per view (e.g., \code{list(rna = "NegativeBinomial")}). Inferred automatically when \code{NULL}. One of \code{"Normal"}, \code{"NegativeBinomial"}, \code{"Bernoulli"}. \cr
   \code{nonnegative_weights} \tab \code{FALSE} \tab Enforce non-negative constraint on weights. \cr
   \code{nonnegative_factors} \tab \code{FALSE} \tab Enforce non-negative constraint on factors. \cr
   \code{annotation_confidence} \tab \code{0.99} \tab Confidence threshold for annotation-guided factors (used when \code{data_options$annotations_varm_key} is set). \cr
   \code{guiding_vars_likelihoods} \tab \code{"Normal"} \tab Likelihood for guiding variable coefficients. One of \code{"Normal"}, \code{"Categorical"}, \code{"Bernoulli"}. \cr
   \code{guiding_vars_scales} \tab \code{1.0} \tab Prior scale for guiding variable coefficients. \cr
   \code{init_factors} \tab \code{"random"} \tab Factor initialisation strategy. One of \code{"random"}, \code{"orthogonal"}, \code{"pca"}, or a numeric value. \cr
   \code{init_scale} \tab \code{0.1} \tab Scale for random factor initialisation. \cr
}
}

\section{Data options}{

All fields are keyword arguments passed verbatim to \code{mofaflex.DataOptions}.\tabular{lll}{
   Field \tab Default \tab Description \cr
   \code{group_by} \tab \code{NULL} \tab Column(s) of \code{.obs} in \code{AnnData}/\code{MuData} used to define groups. Ignored for nested dict input. \cr
   \code{layer} \tab \code{NULL} \tab Which layer to use. \code{NULL} → \code{.X}. A string applies to all views; a named list maps view names to layer names. \cr
   \code{scale_per_group} \tab \code{TRUE} \tab Scale features within each group independently. \cr
   \code{annotations_varm_key} \tab \code{NULL} \tab Key in \code{.varm} containing a binary feature-by-programme annotation matrix. When set, enables annotation-guided factors. \cr
   \code{covariates_obs_key} \tab \code{NULL} \tab \code{.obs} column(s) to include as fixed covariates. \cr
   \code{covariates_obsm_key} \tab \code{NULL} \tab \code{.obsm} key to use as fixed covariates. \cr
   \code{guiding_vars_obs_keys} \tab \code{NULL} \tab \code{.obs} column(s) to use as guiding variables. \cr
   \code{use_obs} \tab \code{"union"} \tab How to align samples across views: \code{"union"} or \code{"intersection"}. \cr
   \code{use_var} \tab \code{"union"} \tab How to align features across groups: \code{"union"} or \code{"intersection"}. \cr
   \code{subset_var} \tab \code{"highly_variable"} \tab \code{.var} boolean column used to subset to highly variable features. \code{NULL} uses all features. \cr
   \code{plot_data_overview} \tab \code{TRUE} \tab Show a data-overview plot before training starts. \cr
   \code{remove_constant_features} \tab \code{TRUE} \tab Remove constant (zero-variance) features before fitting. \cr
}
}

\section{Training options}{

All fields are keyword arguments passed verbatim to
\code{mofaflex.TrainingOptions}.  The \code{mofa_compat} field is \strong{always}
overridden by the top-level \code{mofa_compat} argument.\tabular{lll}{
   Field \tab Default \tab Description \cr
   \code{device} \tab \code{"cuda"} \tab PyTorch device string: \code{"cuda"}, \code{"cpu"}, or \code{"mps"}. Falls back to CPU automatically when no GPU is available. \cr
   \code{batch_size} \tab \code{0} \tab Mini-batch size. \code{0} means full-batch (recommended default). \cr
   \code{max_epochs} \tab \code{10000} \tab Maximum number of training epochs. \cr
   \code{n_particles} \tab \code{1} \tab Number of particles for importance-weighted ELBO. \cr
   \code{lr} \tab \code{0.001} \tab Base learning rate for the Adam optimizer. \cr
   \code{early_stopper_patience} \tab \code{100} \tab Number of epochs without improvement before early stopping. \cr
   \code{save_path} \tab \code{NULL} \tab File path to save the trained model. \code{NULL} does not save. When \code{mofa_compat} is set, the file is saved as an HDF5 readable by \href{https://biofam.github.io/MOFA2/}{MOFA2}. \cr
   \code{seed} \tab \code{NULL} \tab Integer random seed. \code{NULL} → time-based seed. \cr
   \code{num_workers} \tab \code{0} \tab Number of \code{torch.utils.data.DataLoader} workers. \cr
   \code{pin_memory} \tab \code{FALSE} \tab Use pinned (page-locked) memory in the data loader. \cr
}
}

\section{Smooth options}{

Passed to \code{mofaflex.SmoothOptions(...)} only when \code{smooth_options} is
non-\code{NULL} (activates the MEFISTO / GP-factor extension).\tabular{lll}{
   Field \tab Default \tab Description \cr
   \code{n_inducing} \tab \code{100} \tab Number of inducing points for the sparse GP approximation. \cr
   \code{kernel} \tab \code{"RBF"} \tab GP kernel type. One of \code{"RBF"}, \code{"Matern"}. \cr
   \code{mefisto_kernel} \tab \code{TRUE} \tab Use the MEFISTO-style structured kernel. \cr
   \code{independent_lengthscales} \tab \code{FALSE} \tab Fit an independent lengthscale per group. \cr
   \code{group_covar_rank} \tab \code{1} \tab Rank of the inter-group covariance matrix. \cr
   \code{warp_groups} \tab \code{character()} \tab Group names to warp along the covariate axis. \cr
   \code{warp_interval} \tab \code{20} \tab Number of epochs between warping steps. \cr
   \code{warp_open_begin} \tab \code{TRUE} \tab Allow temporal warping at the beginning of the time axis. \cr
   \code{warp_open_end} \tab \code{TRUE} \tab Allow temporal warping at the end of the time axis. \cr
   \code{warp_reference_group} \tab \code{NULL} \tab Name of the reference (unwarped) group. \cr
}
}

\examples{
\dontrun{
## Minimal example with a single AnnData from a SingleCellExperiment
library(scran)
library(scuttle)

sce <- scRNAseq::ZeiselBrainData()
sce <- scuttle::logNormCounts(sce)
hvgs <- scran::getTopHVGs(scran::modelGeneVar(sce), n = 2000)
sce  <- sce[hvgs, ]

adata <- sce_to_anndata(sce, assay = "logcounts")

model <- fit_mofaflex(
  data             = list(group_1 = list(rna = adata)),
  model_options    = list(n_factors = 5L, weight_prior = "Horseshoe"),
  data_options     = list(scale_per_group = FALSE, subset_var = NULL,
                          plot_data_overview = FALSE),
  training_options = list(max_epochs = 2000L, seed = 42L, device = "cpu")
)

## Inspect variance explained
r2 <- reticulate::py_to_r(model$get_r2())
print(r2)

## Extract factor scores as AnnData
factors_ad <- model$get_factors(return_type = "anndata")
}

}
\seealso{
\code{\link[=sce_to_reticulate_anndata]{sce_to_reticulate_anndata()}}, \code{\link[=sce_to_reticulate_mudata]{sce_to_reticulate_mudata()}},
\code{\link[=mae_to_reticulate_mudata]{mae_to_reticulate_mudata()}}, \code{\link[=sce_to_anndata]{sce_to_anndata()}}
}
