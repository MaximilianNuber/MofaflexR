% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_mofaflex_model.R
\name{load_mofaflex_model}
\alias{load_mofaflex_model}
\title{Load a MOFA-FLEX model into MOFA2}
\usage{
load_mofaflex_model(path, ...)
}
\arguments{
\item{path}{Character scalar. Path to the HDF5 file written by
\code{\link[=fit_mofaflex]{fit_mofaflex()}} (with \code{mofa_compat = "full"}).}

\item{...}{Additional arguments forwarded verbatim to
\code{\link[MOFA2:load_model]{MOFA2::load_model()}}.}
}
\value{
A trained \code{MOFA} object as returned by \code{\link[MOFA2:load_model]{MOFA2::load_model()}}.
}
\description{
A thin wrapper around \code{\link[MOFA2:load_model]{MOFA2::load_model()}} that transparently handles an
incompatibility between the HDF5 files written by \strong{mofaflex} and the
HDF5-group detection logic used by MOFA2 1.x.

\strong{Background.} MOFA2's \code{load_model()} calls \code{rhdf5::h5ls()} recursively
and checks whether \code{"covariates"} appears \emph{anywhere} in the resulting name
column — including in deeply-nested paths such as
\verb{/mofaflex/state/data_opts/covariates}. When the string is found it
unconditionally calls \code{h5read(file, "covariates")}, which fails because no
\emph{top-level} \verb{/covariates} group exists in mofaflex output files.

This function detects that situation and writes an empty top-level
\verb{/covariates} entry into a temporary copy of the file before passing it to
MOFA2, so that \code{load_model()} parses it without error.
}
\seealso{
\code{\link[=fit_mofaflex]{fit_mofaflex()}}, \code{\link[MOFA2:load_model]{MOFA2::load_model()}}
}
