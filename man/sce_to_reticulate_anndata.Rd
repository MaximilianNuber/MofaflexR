% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sce_to_reticulate_anndata.R
\name{sce_to_reticulate_anndata}
\alias{sce_to_reticulate_anndata}
\title{Convert a SingleCellExperiment to an anndataR ReticulateAnnData}
\usage{
sce_to_reticulate_anndata(x, assay = "counts", ...)
}
\arguments{
\item{x}{A \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment}.}

\item{assay}{A single string: the assay to place in \code{X} (default
\code{"counts"}).}

\item{...}{Additional arguments forwarded to \code{\link[=sce_to_anndata]{sce_to_anndata()}}.}
}
\value{
An \code{anndataR::ReticulateAnnData} R6 object backed by a Python
\code{anndata.AnnData}.
}
\description{
Converts a \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment} to an
\code{anndataR::ReticulateAnnData} object backed by a Python
\code{anndata.AnnData}.  The main matrix (\code{X}) is a
\code{scipy.sparse.csc_matrix} built without densifying the data; see
\code{\link[=sce_assay_to_scipy_csc]{sce_assay_to_scipy_csc()}} for zero-copy semantics and lifetime caveats.

The returned object behaves as an \code{anndataR} AnnData: it can be passed to
scanpy functions, written to HDF5/\code{.h5ad}, or inspected with the standard
\code{$} accessor.
}
\examples{
\dontrun{
library(SingleCellExperiment)
library(Matrix)

counts <- rsparsematrix(200, 50, density = 0.1, repr = "C")
sce <- SingleCellExperiment(assays = list(counts = counts))

rada <- sce_to_reticulate_anndata(sce)
print(rada)      # shows n_obs x n_vars

# Access the underlying Python object
py_adata <- rada$py_anndata()
cat("Shape:", reticulate::py_str(py_adata$shape), "\n")
cat("X is sparse:", reticulate::py_bool(
  reticulate::import("scipy.sparse")$issparse(py_adata$X)
), "\n")
}

}
\seealso{
\code{\link[=sce_assay_to_scipy_csc]{sce_assay_to_scipy_csc()}}, \code{\link[=sce_to_anndata]{sce_to_anndata()}}
}
