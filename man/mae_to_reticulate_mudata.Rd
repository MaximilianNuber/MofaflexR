% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mae_to_reticulate_mudata.R
\name{mae_to_reticulate_mudata}
\alias{mae_to_reticulate_mudata}
\title{Convert a MultiAssayExperiment to a ReticulateMuData}
\usage{
mae_to_reticulate_mudata(mae, assay = NULL, ...)
}
\arguments{
\item{mae}{A \link[MultiAssayExperiment:MultiAssayExperiment]{MultiAssayExperiment::MultiAssayExperiment}.}

\item{assay}{Character string or \code{NULL}.  Name of the assay to use as \code{X}
for \strong{every} experiment.  If \code{NULL} (default) the first available assay
is used for each experiment individually.}

\item{...}{Additional arguments forwarded to \code{\link[=sce_to_anndata]{sce_to_anndata()}} for each
experiment modality.}
}
\value{
A \link{ReticulateMuData} wrapping a Python \code{mudata.MuData}.
}
\description{
Converts each experiment in a
\code{\link[MultiAssayExperiment:MultiAssayExperiment]{MultiAssayExperiment::MultiAssayExperiment()}} to a Python \code{anndata.AnnData}
modality, then assembles them into a \link{ReticulateMuData}.
\subsection{Required package}{

This function requires the \strong{MultiAssayExperiment} Bioconductor package,
which is listed under \code{Suggests}.  Install it with:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{BiocManager::install("MultiAssayExperiment")
}\if{html}{\out{</div>}}
}

\subsection{Experiment handling}{
\itemize{
\item \link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment} and
\link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment} experiments are converted via
\code{\link[=sce_to_anndata]{sce_to_anndata()}}.
\item Experiments of any other type emit a \code{warning()} and are \strong{skipped}.
}
}

\subsection{Sample (column) metadata}{

The MAE's \code{colData} is joined to each modality's \code{obs} before building
the MuData, so that all per-sample annotations are available globally via
\code{mdata$obs} once \code{mdata$update()} is called.
}
}
\examples{
\dontrun{
# ---- Tiny MAE with two experiments ----
library(MultiAssayExperiment)
library(SingleCellExperiment)
library(Matrix)
library(S4Vectors)

set.seed(1)
n_samples <- 50
sample_ids <- paste0("sample_", seq_len(n_samples))

rna_mat  <- Matrix::rsparsematrix(200, n_samples, 0.1, repr = "C")
rownames(rna_mat) <- paste0("gene_", seq_len(200))
colnames(rna_mat) <- sample_ids

adt_mat  <- Matrix::rsparsematrix(30, n_samples, 0.4, repr = "C")
rownames(adt_mat) <- paste0("protein_", seq_len(30))
colnames(adt_mat) <- sample_ids

sce_rna  <- SingleCellExperiment(assays = list(counts = rna_mat))
sce_adt  <- SingleCellExperiment(assays = list(counts = adt_mat))

sample_df <- S4Vectors::DataFrame(
  condition = sample(c("ctrl", "treat"), n_samples, TRUE),
  row.names = sample_ids
)

el <- MultiAssayExperiment::ExperimentList(rna = sce_rna, adt = sce_adt)
cl <- MultiAssayExperiment::listToMap(
        setNames(lapply(c("rna","adt"), function(nm) {
          data.frame(primary = sample_ids, colname = sample_ids)
        }), c("rna","adt"))
      )
mae <- MultiAssayExperiment::MultiAssayExperiment(
  experiments = el,
  colData     = sample_df,
  sampleMap   = cl
)

mdata <- mae_to_reticulate_mudata(mae)
print(mdata)
mdata$n_obs()
mdata[["rna"]]
mdata[["adt"]]
}

}
\seealso{
\code{\link[=sce_to_reticulate_mudata]{sce_to_reticulate_mudata()}}, \code{\link[=sce_to_anndata]{sce_to_anndata()}}, \link{ReticulateMuData}
}
