% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sce_to_reticulate_mudata.R
\name{sce_to_reticulate_mudata}
\alias{sce_to_reticulate_mudata}
\title{Convert a SingleCellExperiment to a ReticulateMuData}
\usage{
sce_to_reticulate_mudata(
  x,
  main_modality = "rna",
  assay = "counts",
  altexp_assay = NULL,
  obs = list(),
  obsm = list(),
  ...
)
}
\arguments{
\item{x}{A \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment}.}

\item{main_modality}{Character string; name for the main SCE modality.
Default: \code{"rna"}.}

\item{assay}{Character string; assay name to use as \code{X} in the main
modality (passed to \code{\link[=sce_to_anndata]{sce_to_anndata()}}).  Default: \code{"counts"}.}

\item{altexp_assay}{Character string or \code{NULL}.  Assay to use as \code{X} in
each alternative experiment modality.  If \code{NULL} (default) the first
available assay is used for each altExp.}

\item{obs}{Named list of additional columns to add to the modalities' \code{obs}
data frames.  Each element must be a vector/factor of length
\code{ncol(x)}. Forwarded to \code{\link[=sce_to_anndata]{sce_to_anndata()}} for each modality.}

\item{obsm}{Named list of embedding matrices (cells × dims) to attach to
each modality's \code{obsm}. Forwarded to \code{\link[=sce_to_anndata]{sce_to_anndata()}} for the main SCE.}

\item{...}{Additional arguments forwarded to \code{\link[=sce_to_anndata]{sce_to_anndata()}} for
\strong{every} modality (including altExps).  Arguments that differ between
modalities can be set via \code{obs}, \code{obsm}, etc., which only apply to the
main modality.}
}
\value{
A \link{ReticulateMuData} wrapping a Python \code{mudata.MuData}.
}
\description{
Converts a \link[SingleCellExperiment:SingleCellExperiment]{SingleCellExperiment::SingleCellExperiment} (possibly with
alternative experiments attached via \code{\link[SingleCellExperiment:altExps]{SingleCellExperiment::altExps()}}) to
a \link{ReticulateMuData}.

The main SCE is mapped to the modality named \code{main_modality}.  Each
alternative experiment (accessed with
\code{\link[SingleCellExperiment:altExps]{SingleCellExperiment::altExpNames()}}) is mapped to its own modality using
the altExp name as the key.

All matrix transfers are \strong{zero-copy} (via \code{\link[=sce_assay_to_scipy_csc]{sce_assay_to_scipy_csc()}}).
}
\examples{
\dontrun{
# ---- Minimal synthetic CITE-seq example ----
library(SingleCellExperiment)
library(Matrix)

set.seed(42)
n_cells <- 100; n_genes <- 500; n_proteins <- 30

rna_counts <- Matrix::rsparsematrix(n_genes, n_cells, density = 0.1,
                                    repr = "C")
adt_counts <- Matrix::rsparsematrix(n_proteins, n_cells, density = 0.5,
                                    repr = "C")

sce <- SingleCellExperiment(
  assays  = list(counts = rna_counts),
  colData = S4Vectors::DataFrame(cell_type = sample(c("T","B"), n_cells, TRUE))
)
rownames(sce) <- paste0("gene_", seq_len(n_genes))
colnames(sce) <- paste0("cell_", seq_len(n_cells))

adt_sce <- SingleCellExperiment(assays = list(counts = adt_counts))
rownames(adt_sce) <- paste0("protein_", seq_len(n_proteins))
colnames(adt_sce) <- colnames(sce)

SingleCellExperiment::altExp(sce, "adt") <- adt_sce

mdata <- sce_to_reticulate_mudata(sce, main_modality = "rna", assay = "counts")
print(mdata)
mdata$obs_names   # shared cell barcodes
mdata[["rna"]]    # ReticulateAnnData for RNA
mdata[["adt"]]    # ReticulateAnnData for ADT
}

}
\seealso{
\code{\link[=sce_to_anndata]{sce_to_anndata()}}, \code{\link[=mae_to_reticulate_mudata]{mae_to_reticulate_mudata()}},
\link{ReticulateMuData}
}
